import "./Logo.scss";

const Logo = () => {
  return (
    <h1 className="home-title" data-aos="fade-zoom-in" data-aos-duration="1500" data-aos-delay="500">
      <p >
        Performance in every line
      </p>
      <span>
        Power in every detail
      </span>
    </h1>
  );
};

export default Logo;
